# Set this in the shell before npm run dev; no dotenv dependency is used.
ADMIN_TOKEN=replace-with-your-local-admin-key
PORT=2567
# Optional. Empty uses embedded PGlite in PLAYER_DATA_DIR (default data/players).
# DATABASE_URL=postgres://localhost:5432/kartishki
# PLAYER_DATA_DIR=data/players
